// Shared type definitions used across all apps.
// Keep this file focused on cross-app contracts; app-specific
// types live alongside their respective view components.

export type AppName = "calendar" | "contacts" | "mail" | "notes";
